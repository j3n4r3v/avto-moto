import { carInfo } from '../../mocks/cars';

const initialState = {
    carInfo,
};

const ActionType = {
};

const ActionCreator = {
};

const reducer = (state = initialState) => {
    return state;
};

export { reducer, ActionType, ActionCreator };